# schema_runtime_dart

Pure Dart schema runtime core for Daryeel2.

This package owns:
- Typed schema nodes (`SchemaNode`, `ComponentNode`, `RefNode`)
- Parsing helpers (`parseScreenSchema`, `parseFragmentSchema`, ...)
- Reference resolution (`resolveScreenRefs`)
