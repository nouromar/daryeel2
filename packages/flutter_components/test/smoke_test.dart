import 'package:flutter_components/flutter_components.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('flutter_components smoke', () {
    // Verifies the package can be imported and compiled.
    expect(ScreenTemplateWidget, isNotNull);
  });
}
