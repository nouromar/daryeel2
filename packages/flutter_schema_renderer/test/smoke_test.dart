import 'package:flutter_schema_renderer/flutter_schema_renderer.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('flutter_schema_renderer smoke', () {
    // Verifies the package can be imported and compiled.
    expect(SchemaRenderer, isNotNull);
  });
}
