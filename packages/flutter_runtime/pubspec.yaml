name: flutter_runtime
description: Core schema runtime orchestration for Daryeel2.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  schema_runtime_dart:
    path: ../schema_runtime_dart

flutter:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0